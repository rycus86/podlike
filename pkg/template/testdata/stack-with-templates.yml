version: '3.5'
services:

  simple:
    image: sample/app
    labels:
      - example.label=test-label
    ports:
      - 8080:4000
    volumes:
      - source:/var/target:nocopy

x-podlike:
  simple:
#    pod: template/simple/pod.yml
#    transformer: templates/simple/transformer.yml
    templates:
      - templates/simple/templates.yml
#    copy:
#      - templates/simple/copies.yml
